<%= simple_form_for(@submitted_assignment) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.association :assignment %>
    <%= f.input :revision %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
